# Test Environment Configuration
NODE_ENV=test
PORT=3001

# Database Configuration (Test)
SUPABASE_URL=https://test.supabase.co
SUPABASE_ANON_KEY=test-anon-key
SUPABASE_SERVICE_ROLE_KEY=test-service-role-key

# JWT Configuration (Test)
JWT_SECRET=test-jwt-secret-key-for-testing-only
JWT_EXPIRES_IN=1h

# Bcrypt Configuration (Test - Lower rounds for faster tests)
BCRYPT_ROUNDS=4

# Rate Limiting (Test - More lenient)
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100

# File Upload (Test)
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./test/uploads

# Cache Configuration (Test)
CACHE_TTL=300

# Logging (Test)
LOG_LEVEL=error

# External Services (Test - Mock endpoints)
PAYMENT_GATEWAY_URL=https://mock-payment.test
SMS_GATEWAY_URL=https://mock-sms.test
EMAIL_SERVICE_URL=https://mock-email.test

# QR Code Configuration (Test)
QR_CODE_SIZE=200
QR_CODE_MARGIN=4

# Session Configuration (Test)
SESSION_TIMEOUT=3600

# Business Logic (Test)
MEMBERSHIP_GRACE_PERIOD=7
CLASS_BOOKING_DEADLINE=2
WALLET_MIN_BALANCE=0
WALLET_MAX_BALANCE=10000000